-- Hook vào hàm GetCountryRegionForPlayerAsync để giả lập khu vực là Pháp
local GetCountryRegionForPlayerAsync = require(game:GetService("ReplicatedStorage").Library.Functions.GetCountryRegionForPlayerAsync)

-- Hook hàm để luôn trả về "FR" (Pháp) thay vì khu vực mặc định
hookfunction(GetCountryRegionForPlayerAsync, function(player, skipCache, cacheDuration)
    return "FR"  -- Giả lập khu vực là Pháp
end)

-- Tạo GUI để hiển thị khu vực người chơi
local player = game.Players.LocalPlayer

-- Tạo một ScreenGui để chứa thông tin
local screenGui = Instance.new("ScreenGui")
screenGui.Parent = player.PlayerGui

-- Tạo Frame để hiển thị thông tin khu vực
local frame = Instance.new("Frame")
frame.Parent = screenGui
frame.Size = UDim2.new(0, 300, 0, 200)
frame.Position = UDim2.new(0.5, -150, 0.5, -100)
frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
frame.BackgroundTransparency = 0.5

-- Tạo TextLabel để hiển thị khu vực
local label = Instance.new("TextLabel")
label.Parent = frame
label.Size = UDim2.new(1, 0, 1, 0)
label.TextColor3 = Color3.fromRGB(255, 255, 255)
label.TextSize = 24
label.Text = "Khu vực của bạn: Đang tải..."
label.TextWrapped = true

-- Hàm cập nhật thông tin khu vực người chơi
local function updateRegionLabel()
    local region = GetCountryRegionForPlayerAsync(player)  -- Lấy khu vực của người chơi (giả lập là "FR")
    label.Text = "Khu vực của bạn: " .. region  -- Hiển thị khu vực là Pháp
end

-- Sử dụng RunService để gọi updateRegionLabel một lần khi game đã khởi động
game:GetService("RunService").Heartbeat:Once(function()
    updateRegionLabel()
end)
