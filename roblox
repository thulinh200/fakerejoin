-- Hook vào hàm GetCountryRegionForPlayerAsync để giả lập khu vực là Tây Ban Nha (ES)
local GetCountryRegionForPlayerAsync = require(game:GetService("ReplicatedStorage").Library.Functions.GetCountryRegionForPlayerAsync)

-- Hook vào hàm GetCountryRegionForPlayerAsync để luôn trả về "ES" (Tây Ban Nha)
hookfunction(GetCountryRegionForPlayerAsync, function(player, skipCache, cacheDuration)
    return "ES"  -- Giả lập khu vực là Tây Ban Nha
end)

-- Tạo GUI để hiển thị khu vực người chơi
local player = game.Players.LocalPlayer

-- Tạo một ScreenGui để chứa thông tin khu vực
local screenGui = Instance.new("ScreenGui")
screenGui.Parent = player.PlayerGui

-- Tạo Frame để hiển thị thông tin khu vực
local frame = Instance.new("Frame")
frame.Parent = screenGui
frame.Size = UDim2.new(0, 300, 0, 200)
frame.Position = UDim2.new(0.5, -150, 0.5, -100)
frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
frame.BackgroundTransparency = 0.5

-- Tạo TextLabel để hiển thị khu vực
local label = Instance.new("TextLabel")
label.Parent = frame
label.Size = UDim2.new(1, 0, 1, 0)
label.TextColor3 = Color3.fromRGB(255, 255, 255)
label.TextSize = 24
label.Text = "Khu vực của bạn: Đang tải..."
label.TextWrapped = true

-- Cập nhật khu vực chỉ một lần, không cần cập nhật liên tục
local function updateRegionLabel()
    local region = GetCountryRegionForPlayerAsync(player)  -- Lấy khu vực người chơi (giả lập là "ES")
    label.Text = "Khu vực của bạn: " .. region  -- Hiển thị khu vực "ES" (Tây Ban Nha)
end

-- Cập nhật GUI ngay khi game bắt đầu mà không cần cập nhật mỗi frame
updateRegionLabel()
